# Журнал проекта «Золотой бот»

Накопительная запись по золотому боту: что это, какие решения приняты, источники данных,
инфраструктура, деплой, сделанное и открытые задачи.

## 1. Что за бот

Телеграм-бот для торговли ломом золота (Артём покупает лом — кольца, цепи, серьги — и продаёт изделия).
Задачи бота:
- показывать курс золота (ЦБ РФ + Сбербанк) закреплённым сообщением;
- слать новости про драгметаллы (подталкивают покупать);
- вести журнал сделок в Google Sheets по кнопкам.

Telegram: **@Kof_gold_Bot** (Gold_Bot).

## 2. Принятые решения (развилки и итог)

1. **Отдельный репозиторий** `skyfallcoin63/Gold-Bot` (создан вручную — из приложения GitHub не даёт, 403).
2. **Проба — только 585** (покупает и продаёт 585). В журнале проба фиксируется в строке.
3. **Обновление курса — 1 раз в день утром** (`PIN_HOUR`), процент за день по двум последним торговым дням ЦБ.
4. **Без унции** — только рубли и граммы.
5. **Закреп** — состав строк (итог после всех правок):
   ```
   🟡 Золото — курс на ДД.ММ.ГГГГ (ЦБ РФ)
   💵 Доллар ЦБ: X ₽
   🥇 999 (ЦБ): X ₽/г  ▲/▼ % за день
   🏦 Сбербанк 999: покупка X / продажа X ₽/г
   🏦 Сбербанк 585: покупка X / продажа X ₽/г
   ```
   Процент — только у строки 999. Сбербанк 585 = Сбер 999 × 0.585 (производная, не офиц. котировка Сбера).
6. **Закреп — в личном чате с ботом** (где нажата кнопка «📌 Обновить закреп», там и закрепляется).
   Новости — в группу (`GROUP_CHAT_ID`).
7. **Новости** — несколько раз в день (`NEWS_INTERVAL_HOURS`, дефолт 4 ч) из RSS investing, фильтр по
   драгметаллам, дедуп, + строка-призыв (`NEWS_CTA`).
8. **Журнал сделок** — одна книга Google Sheets, одна вкладка «Журнал» по дням; кнопки «Купить»/«Продать»:
   ввод веса и цены за грамм → бот считает сумму, пишет строку. Колонки A:I: Дата · Тип · Проба ·
   ЦБ 999 ₽/г · Вес г · Цена ₽/г · Сумма ₽ · **Сбер 585 прод. ₽/г** · **Разница ₽/г** (Сбер 585 прод −
   цена сделки, маржа относительно Сбера). Средний курс покупки/продажи (взвешенный: Σ₽ / Σг) — в шапке
   (K:L), значениями (локале-независимо). Ячейки по центру.

## 3. Источники данных (проверены с сервера Артёма, Казахстан)

| Данные | Источник | Статус |
| --- | --- | --- |
| Доллар ЦБ | `cbr.ru/scripts/XML_daily.asp` (Valute R01235) | ✅ 200 |
| Золото 999 ₽/г | `cbr.ru/scripts/xml_metall.asp` (Record Code=1, win-1251, запятая-десятичный) | ✅ 200 |
| Сбербанк 999 покупка/продажа | зеркало `investzoloto.ru/gold-sber-oms/` (числа в сыром HTML) | ✅ автоподтяжка |
| Новости | `ru.investing.com/rss/news_11.rss` | ✅ живой |

Важно: **сайт Сбербанка (sberbank.ru) с сервера НЕ доступен** (гео-блок, connect=000) — поэтому курс Сбера
берём с зеркала investzoloto. Зеркало неофициальное: при смене вёрстки автоподтяжка может отвалиться,
тогда работает запасной **ручной ввод** курса Сбера (кнопка «🏦 Курс Сбера»).

## 4. Инфраструктура

| | Значение |
| --- | --- |
| Репозиторий | `skyfallcoin63/Gold-Bot`, ветка `main` |
| Папка на сервере | `/root/gold-bot/` |
| Сервис systemd | `gold-bot` |
| Venv | `/root/gold-bot/venv/` |
| Ключ Google | `google-key.json` (сервис-аккаунт `postavki-bot@postavki-bot.iam.gserviceaccount.com`, общий с др. ботами) |
| Не в git | `config.py`, `google-key.json`, `state/` (см. `.gitignore`) |
| Сервер | Казахстан `31.130.153.65` (общий с юнит- и постав-ботами) |

Данные в `config.py` (только на сервере): токен, `GROUP_CHAT_ID = -5350784400`,
`JOURNAL_BOOK_ID = 1R2dtObvLBputttDS-6-V8M1t0R3i6aG7cSa52ARNZjI`, `ADMIN_IDS`.

## 5. Файлы

| Файл | Назначение |
| --- | --- |
| `gold_bot.py` | Бот (aiogram v3): кнопки, FSM сделок, закреп, планировщики закрепа/новостей, `/id` |
| `rates.py` | Курсы: ЦБ доллар/золото + Сбер с зеркала; сборка текста закрепа. Офлайн-самопроверка |
| `news.py` | RSS-новости: загрузка, фильтр по драгметаллам, дедуп. Офлайн-самопроверка |
| `journal.py` | Журнал сделок в Google Sheets (gspread), взвешенные средние. Офлайн-самопроверка |
| `config.py` / `config.example.py` | Настройки (реальный не в git) |
| `deploy.sh` | Деплой: git pull → md5 → py_compile → restart |
| `gold-bot.service` | systemd-юнит |

## 6. Деплой

Поток: правка в код → (для существенного) ветка → PR → мерж в `main` → на сервере `./deploy.sh`.
На этапе первичной установки правки лились прямо в `main`.

Команда обновления на сервере:
```
cd /root/gold-bot && ./deploy.sh
```
Скрипт: проверяет незакоммиченные правки, тянет `main`, сверяет md5 до/после, py_compile, перезапуск,
показывает лог. Если код не менялся — не перезапускает.

Первичная установка (один раз): `git clone` → `python3 -m venv venv` →
`venv/bin/pip install -r requirements.txt` → скопировать ключ в `google-key.json` →
`config.py` из шаблона → `gold-bot.service` в `/etc/systemd/system/` → `systemctl enable --now gold-bot`.

## 7. Сделано

- Бот написан, все модули покрыты офлайн-самопроверками (парсинг курса, фильтр новостей, расчёт средних).
- Источники данных проверены вживую (доллар 81.41, золото 999 ≈ 10 627 ₽/г, Сбер 10 109/10 743).
- Развёрнут на сервере, сервис `gold-bot` active (running), автозапуск включён.
- Закреп работает в личном чате; курс собирается корректно.

## 8. Открытые задачи

- **`ADMIN_IDS`** — вписать личный id Артёма (узнать `/id` в личке), чтобы «Купить/Продать» были только у него.
- **Журнал сделок** — расшарить книгу `1R2dtObv…` на сервис-аккаунт (Редактор), затем проверить «🟢 Купить».
- **Новости в группу** — добавить бота в группу с покупателями админом; проверить постинг и id группы
  (в настройках `-5350784400`; у супергрупп бывает формат `-100…` — сверить через `/id` в группе).
- **Автоподтяжка Сбера** — следить: зеркало неофициальное, при поломке использовать ручной ввод.

## 9. История правок

1. Каркас бота: `rates.py`, `news.py`, `journal.py`, `gold_bot.py`, `deploy.sh`, конфиг, README.
2. Добавлен `gold-bot.service` (systemd-юнит) для установки на сервере.
3. В закреп добавлена покупка Сбербанк 585 (= Сбер 999 покупка × 0.585) — показываем покупку и продажу.
4. Закреп переведён в чат, где нажата кнопка (по умолчанию личка), а не только в группу;
   `pin.json` хранит `chat_id`+`message_id`, планировщик использует сохранённый чат.
5. В журнал добавлены колонки «Сбер 585 прод. ₽/г» и «Разница ₽/г» (Сбер 585 продажа − цена сделки);
   сводка средних переехала в K:L; при сделке бот берёт курс Сбера 585 на дату и пишет разницу.
6. Самопочинка шапки журнала: старая разметка (без новых колонок / со сводкой в I-J) автоматически
   переписывается на актуальную A:I, старая сводка в колонке J чистится. Добавлена кнопка
   «🧮 Разница с Сбером» — вводишь цену покупки, бот считает разницу с курсом Сбербанка 585
   (без записи в журнал).
7. Калькулятор оставлен только на разницу с ПРОДАЖЕЙ Сбера (лом vs слиток) + вердикт
   «выгоднее брать лом / купить слиток».
8. Журнал переведён на ФОРМУЛЫ (ручные правки теперь пересчитываются): «Разница ₽/г» — формула
   строки (Сбер 585 прод − Цена), средний курс покупки/продажи — формулы по колонкам
   (взвешенные Σ₽/Σг). Разделитель формул выбирается по локали книги (ru → «;»).
9. Добавлена колонка J «Цена продажи +N ₽/г» = Цена закупа + N (заработок N ₽/г с грамма, только
   для строк «Покупка»); N = config.PROFIT_PER_GRAM (дефолт 500). Формула строки. Сводка средних
   отодвинута в N:O (чтобы колонки не наезжали). Итоговые колонки журнала: A:J.
10. Кнопка «📈 Анализ рынка» — золото и серебро (999) на истории ЦБ за 180 дней: изменение за
    7/30/90/180 дней, диапазон и позиция в нём, тренд 90д, и конкретные цены **заход (закуп) /
    выход (продажа)** = нижняя и верхняя трети диапазона ЦБ. Помечено как ориентир, не прогноз.
    Функции в rates.py (fetch_metals_history, analyze_series, render_market), 1 запрос к xml_metall.
    (Соотношение золото/серебро убрано по просьбе.)
11. В «Анализ рынка» добавлена строка «Сигнал» — соединяет позицию в диапазоне и свежий импульс
    (7д): низ+разворот вверх → «можно заходить»; низ, но ещё падает → «подождать»; верх+вниз →
    «время продавать»; верх → «осторожно». Словесный вывод поверх цифр.
